import React from 'react';

//to initialize component
//creates an instance of 'app' ....react.component lives within the modules
class App extends React.Component{
  //how do we display the data: through built in render
  render(){
    return (
      <div>Hello</div>
    );
  }
}
